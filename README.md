# Canvas patterns

Easily generate `CanvasPattern` objects for use in data graphics.

### Possible API Format

    pattern.format('svg');
    pattern.draw('circle', '#333');

    pattern
      .format('svg')
      .draw('circle', '#333');

    pattern
      .draw('circle', '#333');
